# 編集ガイド（VS Code用）

`index.html` 1枚だけで完結しています。CSSもJSもこのファイルの中にあります。

- `1〜623行` … CSS（`<style>` の中）
- `626行〜` … HTML（本文）
- `末尾` … JavaScript（スクロール演出・FAQ開閉・追従ボタン）

---

## SAMPLE透かしについて

提案用に、ページ全面へ斜めの `SAMPLE` 透かしと左上のバッジを入れてあります。

**外し方は1箇所だけです。** 630行目の

```html
<body class="is-sample">
```

を

```html
<body>
```

に変えるだけ。透かしとバッジが同時に消えます。**戻したいときは書き戻すだけ**なので、
提案中は透かしあり／受注後は透かしなし、を何度でも切り替えられます。

---

## 差し替えが必要な箇所（本番公開前に必ず）

行番号は目安です。ズレたら `Ctrl+F`（Mac は `Cmd+F`）で検索してください。

| 行 | 検索キーワード | やること |
|---|---|---|
| **639** | `is-sample` | **`<body class="is-sample">` の `class="is-sample"` を削除**。SAMPLE透かしと左上バッジが両方消えます |
| 650 / 667 / 913 / 932 | `href="#entry"` | 公式LINEの友だち追加URLに差し替え。**特典カードの741/746/751行も同様** |
| 757 | `12,800円相当` | 特典の通常価格。実際の設定に合わせる |
| 796 / 807 / 818 | `vc-face` | お客様の声の顔アイコン。`images/voice-0N.webp` を差し替えるだけ |
| 798-800 付近 | `class="who"` | お客様の声の属性（40代・女性・会社員 など） |
| 802 付近 | `<h4>` | お客様の声の見出しと本文。**すべてサンプル文** |
| 6 | `<title>` | ブラウザのタブに出るタイトル |

実績数値（1,200名+ / 12年 / 96%）は `images/profile.webp` に焼き込まれています。
変更するには画像の作り直しが必要です。

### 注意

**実績数値とお客様の声は、必ず事実に基づくものに差し替えてください。**
仮のまま公開すると景品表示法上の問題になります。

---

## CTAリンクの差し替え方（3箇所）

```html
<!-- 変更前 -->
<a class="cta" href="#entry">

<!-- 変更後 -->
<a class="cta" href="https://lin.ee/xxxxxxx">
```

VS Code なら `Ctrl+Shift+H`（Mac は `Cmd+Shift+H`）で一括置換できます。
検索：`href="#entry"` → 置換：`href="https://lin.ee/xxxxxxx"`

ただし **「ご参加は、3ステップ」セクションへのアンカー** として使いたい場合は、
FVのボタン（633行）だけ `#entry` のまま残す運用もアリです。

---

## 画像の差し替え方

**HTMLを編集する必要はありません。** `images` フォルダに同じ名前で上書きするだけです。

ファイル名の一覧は [README.md](README.md) にあります。

---

## 色を変えたいとき

CSSの先頭（17行目）の `:root{ }` の中の値を変えるだけで、
ページ全体の色が連動して変わります。

```css
--ink:#1B1233;      /* 濃い紫紺（背景・見出し） */
--gold:#C0983F;     /* 金（アクセント・ボタン） */
--pink:#E39BC2;     /* ピンク */
--violet:#8B6BC7;   /* 紫 */
```

---

## プレビューの見方（VS Code）

1. 拡張機能で **Live Server** をインストール
2. `index.html` を右クリック → **Open with Live Server**
3. ブラウザが開く。ファイルを保存すると自動でリロードされます

スマホ表示の確認は、ブラウザで `F12` → 左上のスマホアイコン → `iPhone 14 Pro` を選択。

---

## 変更をGitHubに反映する

VS Code 左端の **ソース管理**（枝アイコン）から：

1. 変更したファイルの `+` を押す（ステージング）
2. 上の入力欄にメッセージを書く（例：`CTAリンクを差し替え`）
3. **コミット** → **変更の同期**

GitHub Pages を有効にしている場合、1分ほどで公開サイトに反映されます。
